# hemi setup

to run chuck with 6 output channels:
```
chuck -c6 program.ck
## in program use different channels
dac.chan(0 through 5) instead of dac.left or dac.right.
```

Start code at:

https://slork.stanford.edu/ck/starter-code